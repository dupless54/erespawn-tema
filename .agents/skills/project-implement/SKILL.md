# Project Implement

Implement only inside the locked path set.

- Prefer supported Discourse theme APIs, outlets, transformers, settings, core variables, and viewport/container helpers.
- Keep selectors stable and specificity low.
- Build mobile-first and preserve keyboard/focus semantics.
- Preserve avatar decorations, nameplates, badges, user-card/profile cosmetics, and optional-plugin markup.
- Keep motion subtle and honor `prefers-reduced-motion`.
- Avoid backend behavior in the theme.
- Make the smallest coherent change, then run targeted validation before expanding.
- Update `docs/ai/CURRENT_STATE.md` when the repository's actual implementation state changes.
