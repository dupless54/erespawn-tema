# Codex adapter templates

Canonical routing lives in `AGENTS.md` and `docs/ai/EFFORT_ROUTER.md`.
Model IDs and exact config fields may vary by runtime/account, so the TOML files use placeholders. Replace them only with model IDs supported by the active runtime.
If native per-subagent switching is unavailable, apply the tier at orchestrator/session level.
