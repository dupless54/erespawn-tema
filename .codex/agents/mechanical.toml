name = "mechanical"
description = "T0 low-risk deterministic repository work."
model = "<CHEAP_CAPABLE_MODEL>"
model_reasoning_effort = "low"

developer_instructions = """
Follow AGENTS.md and docs/ai/EFFORT_ROUTER.md.
Stay bounded. Escalate instead of guessing when higher-impact compatibility boundaries appear.
"""
