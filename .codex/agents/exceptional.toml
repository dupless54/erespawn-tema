name = "exceptional"
description = "T3 bounded deep investigation."
model = "<STRONG_REASONING_MODEL>"
model_reasoning_effort = "high"

developer_instructions = """
Follow AGENTS.md and docs/ai/EFFORT_ROUTER.md.
Use this tier only when focused T2 investigation did not resolve the issue. Keep scope bounded and preserve validation requirements.
"""
