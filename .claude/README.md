# Claude Code adapter

Canonical policy remains in root `AGENTS.md` and `docs/ai/EFFORT_ROUTER.md`.
These are thin runtime adapters. Keep project/theme invariants out of them.
If a Claude Code version does not support a frontmatter key, remove only that unsupported runtime key; preserve the tier policy.
